import { RadiusCalculator } from "@/components/RadiusCalculator";

export default function RadiusCalculatorPage() {
  return (
    <main className="min-h-screen">
      <RadiusCalculator />
    </main>
  );
}
